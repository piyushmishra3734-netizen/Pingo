/**
 * @deprecated Prefer `AiProfileSheet` — full contact-style editor.
 * Kept as a thin alias so older imports keep working.
 */
export { AiProfileSheet as AiSettingsSheet } from './AiProfileSheet.js';
